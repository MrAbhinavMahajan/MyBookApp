import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  containerStyle: {
    minHeighteight: 20,
    width: undefined,
    flexDirection: 'row',
  },
  textInputStyle: {
    width: '100%',
  },
});

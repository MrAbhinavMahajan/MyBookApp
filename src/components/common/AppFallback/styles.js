import {StyleSheet} from 'react-native';
import {STYLES} from '../../../utilities/Styles';

export const styles = StyleSheet.create({
  containerView: {
    ...STYLES.flex01,
    ...STYLES.flexItemsFullyCenter,
  },
});
